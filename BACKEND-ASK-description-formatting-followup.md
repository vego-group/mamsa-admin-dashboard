# متابعة `description` — ثلاثة طلبات مفتوحة

**من:** لوحة الأدمن (`mamsa-admin-dashboard`)
**إلى:** فريق الباك-إند
**التاريخ:** 2026-08-26
**ردّاً على:** `MAMSA-BACKEND-REPLY-description-formatting.md` (نفس اليوم)

شكراً على الردّ — كان دقيقاً وكامل، وأهمّ ما فيه شيء لم نسأل عنه أصلاً (`strip_tags`).
نفّذنا كل ما يخصّنا. بقيت **ثلاثة طلبات**، اثنان منها إجابة على سؤال طرحتموه أنتم.

---

## ⛔ الطلب ١ — نشر التغييرات على الإنتاج (يحجب الاستخدام الفعلي)

قلتم إنّ التغييرين على **staging** فقط، وأن نشر الإنتاج ينتظر كلمة.

**نطلب النشر في أقرب نافذة متاحة**، والسبب ليس أننا مستعجلون على الحدّ الجديد:

> **الإنتاج ما زال يشغّل `strip_tags` على `description` الآن.**
> أي وصف يُكتب اليوم على الإنتاج ويحتوي `<` — مثل `المساحة <= 100 متر` — يفقد جزءاً من
> نصّه **داخل العمود**، لا في العرض. الضرر غير قابل للاسترجاع، ولا يظهر لأحد: المراجع يرى
> وصفاً «سليماً» أقصر بسطر، ولا شيء يدلّ على أن شيئاً حُذف.

كل يوم تأخير هو نافذة إضافية لتلف صامت في بيانات حقيقية.

**ونتّفق معكم أن التغييرين ينزلان معاً** — رفع الحدّ وحده يسمح بأوصاف أطول بينما `strip_tags`
ما زال يقضم منها، وهو أسوأ من الوضع الحالي.

**من جهتنا لا يوجد ما ننتظره:** الواجهة تعمل على 500 و2000 معاً (الفرق أن الأدمن يرى خطأ
حقل بدل رفض خادم). `.env.local` عندنا يشير إلى staging، وسنحدّثه ونعيد الاختبار بعد النشر.

**المطلوب:** تاريخ النشر التقريبي.

---

## 🔵 الطلب ٢ — نعم، أزيلوا `strip_tags` من `name` و`district` و`address`

عرضتم ذلك في §3 من ردّكم. **الجواب: نعم من فضلكم، والثلاثة معاً.**

السبب ليس أن هذه الحقول تحمل علامات تنسيق — لا تحمل — بل أن **سلوك الحذف الصامت نفسه** هو
المشكلة:

- **`address`** هو الأكثر ترجيحاً لاحتواء `<`. وصف موقع مثل `<200م من المسجد` أو
  `<5 دقائق من المطار` يفقد باقي السطر بلا أثر، والعنوان هو الحقل الذي يعتمد عليه الضيف
  للوصول فعلياً.
- **`name`** و**`district`** احتمالهما أقلّ بكثير، لكن تكلفة الإزالة صفر، والمكسب هو **قاعدة
  واحدة مفهومة** بدل «هذا الحقل يُنظَّف وذاك لا» — وهي بالضبط القاعدة التي غابت عن الجميع
  حتى اكتشفتموها اليوم.

**لا شيء في لوحة الأدمن يعتمد على `strip_tags` في أيٍّ من الثلاثة.** كلها تُعرض كعُقَد نصّ،
تماماً مثل `description`.

---

## ❓ الطلب ٣ — كيف نفرّغ `amenities`؟ (سؤال جديد)

ظهر أثناء تنفيذ المسح بـ `null` الذي وثّقتموه في §5، وليس في مراسلاتنا السابقة.

§5 غطّت `description` و`address` و`tourismLicenseNumber` — وكلها **نصوص**، فـ `null` واضح
لها. لكن `amenities` **مصفوفة**، والإفراغ فيها له إملاءان محتملان ولم يُوثَّق أيّهما:

```jsonc
{ "amenities": [] }      // ؟
{ "amenities": null }    // ؟
```

### الحالة الفعلية اليوم

لوحة الأدمن **لا ترسل أيّاً منهما**. أي أنّ الأدمن الذي يزيل كل المرافق من وحدة:

1. يرى الشرائح فارغة على الشاشة،
2. يحفظ،
3. تعود المرافق كما كانت عند إعادة التحميل — بلا رسالة خطأ.

اخترنا التخطّي المتعمّد بدل التخمين، لأن تخمين قاعدة غير موثّقة هو بالضبط ما أنتج
`max:500` الوهمي وعاش شهراً. الثغرة مثبّتة عندنا باختبار يوثّقها حتى تصلنا إجابتكم.

### المطلوب — أحد ثلاثة

| الجواب | ما سنفعله |
|---|---|
| `[]` يُفرّغ | نرسل `[]` ونضيف اختباراً |
| `null` يُفرّغ | نرسل `null` ونضيف `amenities` إلى قائمة الحقول القابلة للمسح |
| وحدة بلا مرافق ممنوعة | نمنعها في الواجهة برسالة واضحة بدل السماح بها ثم ابتلاعها |

**ملاحظة:** لو كان الجواب `[]`، نرجو التأكيد أنه لا يُخلط بـ «لم يتغيّر» — لأن `photoFileIds`
له نفس الشكل ووثّقتم أنّ المصفوفة فيه **تستبدل المجموعة كاملة**، فنفترض نفس المعاملة هنا.

---

## 📋 للعلم فقط — ما تغيّر عندنا (لا يحتاج إجراءً منكم)

- **المسح بـ `null` منفَّذ** لـ `description` و`address` و`tourismLicenseNumber`، وعلى هذه
  الثلاثة فقط. اخترنا `null` لا `""` رغم أن الاثنين يعملان: `""` يعني «قيمة فارغة» و`null`
  يعني «لا قيمة»، والثاني هو المقصود — ويبقى صحيحاً لو تغيّر
  `ConvertEmptyStringsToNull` يوماً.
- **`MAX_DESCRIPTION = 2000`** في الواجهة، وحُدّث التعليق ليذكر أن الرقم **مؤكَّد منكم** لا
  مُخمَّن، مع الإشارة إلى أن الإنتاج ما زال على 500.
- **`amenities` تُرسَل الآن مرتّبة أبجدياً.** تغيير في الشكل على السلك قد تلاحظونه في
  السجلات. السبب من طرفنا بحت: كنّا نقارن التغيير بـ `JSON.stringify` وهي مقارنة حسّاسة
  للترتيب، فكان الأدمن الذي يلغي مرفقاً ثم يعيده يُنتج «تغييراً» وهمياً يُرجع وحدة معتمدة
  إلى `pending_review` بلا سبب. **`photoFileIds` لم تُرتَّب** — ترتيبها هو ترتيب المعرض.
- **الحدّ الأدنى 10** نعامله كشرط تقديم لا شرط حفظ، مطابقةً لما وصفتموه: «حفظ كمسودة» لا
  يفحص الوصف، ومسار التقديم يفحصه.
- **اختبار انحدار** يثبّت الشكل الذي كان `strip_tags` يتلفه: `المساحة <= 100 متر` يليها
  `> ملاحظة` يجب أن تُقرأ فقرة ثم صندوق ملاحظة، لا كتلة واحدة.

---

## نموذج ردّ سريع

```text
1. تاريخ نشر الإنتاج (الحدّ 2000 + إزالة strip_tags):  ____________

2. إزالة strip_tags من name/district/address:          نعم / لا
   (إن نعم — نفس تاريخ النشر أعلاه؟           نعم / تاريخ آخر: ______)

3. إفراغ amenities يتم بإرسال:    []  /  null  /  غير مسموح
   وإن كان []: هل يُقرأ كـ«استبدال بالمجموعة الفارغة» لا كـ«لم يتغيّر»؟   نعم / لا
```

---

**سياق كامل:** `BACKEND-REQUEST-description-formatting.md` (الطلب الأصلي) و
`MAMSA-BACKEND-REPLY-description-formatting.md` (ردّكم).
