# طلب للباك-إند — حقل `description`: رفع الحدّ إلى 2000، والحفاظ على الأسطر

**من:** فرق الواجهات الثلاث — موقع ممسى (`mamsa-app`)، لوحة الأدمن (`mamsa-admin-dashboard`)، لوحة الشريك (`mamsa-partner-dashboard`)
**التاريخ:** 2026-08-26
**الأولوية:** 🔴 يحجب إطلاق التنسيق الجديد
**يخصّ:** حقل `description` على مسارات الوحدات — مسارات الأدمن ومسارات الشريك والقراءة العامة معاً (التفصيل في §6)

---

## الخلفية

موقع ممسى (`mamsa-app`) صار يعرض `description` بتنسيق **مبني على الأسطر**، بعلامات بسيطة
يكتبها الشريك أو فريق ممسى كنصّ عادي:

```text
## ما يميّز المكان
*مسبح خاص*
*تسجيل دخول ذاتي*

## المساحات
- **غرفة النوم:** سرير كينج مع دولاب واسع.
- **الصالة:** جلسة عائلية وتلفزيون بشاشة مسطّحة.

## طريقة الوصول
1. اخرج من البوابة الشمالية.
2. اتجه يميناً 400 متر.

> تسجيل الدخول بعد الساعة 3 عصراً.
```

الحقل **يبقى نصّاً عادياً** — لا HTML، ولا Markdown كامل، ولا محرّر WYSIWYG. التحويل كله
يحدث في الواجهة عند العرض. لوحة الأدمن نفّذت نفس المُحلِّل حتى تعرض للمُراجِع ما سيراه الضيف،
ولوحة الشريك ستفعل المثل في تبويب المعاينة.

**لا يُطلب منكم تنفيذ أي تحليل أو تحويل.** المطلوب شيء واحد في جوهره: أن يصل النصّ إلينا
كما كتبه صاحبه، بايتاً بايت.

---

## 1. الأسطر الجديدة (`\n`) — الأهم

**العلامات لا تعني شيئاً إلا في بداية السطر.** لو انهارت الأسطر في أي مكان بين الحفظ
والاسترجاع، ينهار التنسيق كله: `## المساحات\n- غرفة النوم` تتحوّل إلى فقرة واحدة بلا عنوان
ولا قائمة، والوصف يصل الضيف كجدار نصّ رمادي.

**المطلوب تأكيده:**

| السؤال | ما نفترضه |
|---|---|
| هل مسارات الإنشاء والتعديل (الأدمن والشريك، §6) تخزّن `description` كما وصلت؟ | نعم — بلا `trim` داخلي، بلا `preg_replace('/\s+/', ' ')`، بلا `strip_tags`، بلا تحويل `\n` إلى `<br>` |
| هل مسارات القراءة (الأدمن والعامة، §6) تُرجعها كما خُزّنت؟ | نعم — نفس البايتات |
| هل يمرّ الحقل على أي `sanitize` / `normalize` في الميدل-وير؟ | لا |
| عمود قاعدة البيانات | `TEXT` وليس `VARCHAR(500)` — راجع النقطة 2 |

من طرفنا: اللوحات تطبّق `trim()` على الأطراف فقط (سطر فارغ في البداية أو النهاية لا
يحمل معنى)، ولا تلمس أي شيء بين السطر الأول والأخير. هذا موثّق في
`src/lib/units/wizard.ts` عند `toCreateBody`.

---

## 2. رفع الحدّ من 500 إلى 2000 حرف

الحدّ 500 **لم يأتِ منكم أصلاً**. في `BACKEND-REQUEST-mamsa-owned-units.md` §8.4 كتبنا:

> «نحاكي ما نعرفه من قواعد الشريك في الواجهة حتى يرى الأدمن خطأ حقل بدل رفض من الخادم:
> `description` 10–500 … أرجو التأكيد — أو أرسلوا القائمة الحقيقية»

ولم يصلنا ردّ على هذه النقطة. أي أن 500 كان **تخميننا نحن**، لا قاعدتكم.

وصف منظّم بعناوين وبطاقات مميزات وقوائم لا يتّسع في 500 حرف — المثال أعلاه وحده يتجاوزها.
**رفعنا الحدّ في الواجهة إلى 2000 حرف** (`MAX_DESCRIPTION` في `src/lib/units/wizard.ts`).

**المطلوب:**

1. أخبرونا بالحدّ الفعلي على الخادم لـ `description` — **على مسارات الأدمن ومسارات الشريك
   معاً** (§6). لو كان الرقمان مختلفين، أرسلوا الاثنين.
2. **إن كان `max:500`** — الرجاء رفعه إلى `max:2000`، وإلا فكل وصف منسّق سيُرفض بـ
   `422 VALIDATION_ERROR` على آخر خطوة في نموذج من خمس خطوات، وهو أسوأ مكان ممكن للرفض.
3. تأكيد أن الحدّ يُحسب بـ **الأحرف** لا بالبايتات: الحرف العربي ثلاث بايتات في UTF-8،
   فـ `strlen()` بدل `mb_strlen()` يعني أن الحدّ الحقيقي ~666 حرفاً عربياً لا 2000.
   عدّاد الأحرف الذي يراه الكاتب يعدّ الأحرف، والسطر الجديد حرف واحد.
4. تأكيد أن الحدّ الأدنى ما زال 10 أحرف (لم نغيّره).

---

## 3. لا تحويل إلى HTML — من الطرفين

- الواجهة لا تستخدم `dangerouslySetInnerHTML` إطلاقاً؛ المُحلِّل يُخرج عُقَد نصّ فقط، فوصف
  يحتوي `<script>` يُعرض كثمانية أحرف حرفية.
- بالمقابل: **لا تحوّلوا `\n` إلى `<br>` عند التخزين ولا عند الإرجاع**. لو وصلنا HTML
  سيُعرض للضيف كنصّ حرفي `<br>` — وهو أسوأ من لا شيء.
- **وتحديداً: لا `htmlspecialchars` / `htmlentities` عند التخزين.** علامة الملاحظة عندنا
  هي `>` في بداية السطر. أي هروب HTML وقت الحفظ يحوّلها إلى `&gt;`، فيرى الضيف
  `&gt; تسجيل الدخول…` نصّاً حرفياً بدل صندوق ملاحظة — والحقل يتلف في قاعدة البيانات
  لا في العرض فقط، أي أن إصلاحه لاحقاً يحتاج هجرة بيانات. الهروب مكانه **وقت العرض**،
  وهو مضمون عندنا أصلاً لأننا نبني عُقَد نصّ لا HTML.
- الأحرف التي يجب أن تعبر كما هي: `#` `*` `-` `>` `»` `•` `–` `—` `\n`.

---

## 4. التوافق الخلفي

أي وصف قديم بلا علامات يمرّ كفقرة عادية ويُعرض تماماً كما كان. لا حاجة لأي هجرة بيانات،
ولا لعمود جديد، ولا لعَلَم `format`.

---

## 5. كيف نمسح حقلاً اختيارياً؟ (سؤال مفتوح)

ظهر أثناء مراجعة مسار الحفظ، ويخصّ `description` و`address` و`tourismLicenseNumber` معاً.

`PATCH` يقرأ المفتاح الغائب على أنه «لم يتغيّر». فلو أفرغ الأدمن حقلاً اختيارياً، ليس
لدينا طريقة موثّقة لقول «امسحه» بدل «اتركه كما هو» — إرسال `undefined` يختفي من
الـ JSON، والنتيجة طلب فارغ `{}` لا يفعل شيئاً.

عالجنا الضرر من طرفنا: `toPatchBody` لم يعد يعدّ الحقل المُفرَغ تغييراً، فلم يعد
يُرسَل طلب لا يغيّر شيئاً — وهو ما كان يُعيد وحدةً معتمدة إلى `pending_review` بلا سبب.
لكن **المسح نفسه ما زال غير ممكن**.

**المطلوب:** ما الذي يقبله الخادم لمسح حقل اختياري — `null`؟ سلسلة فارغة `""`؟ أم أن
المسح ممنوع أصلاً على هذه الحقول؟ لو كان `null` هو الجواب، سنرسله ونضيف اختباراً له.

(لـ `description` تحديداً: هل الحدّ الأدنى 10 أحرف يعني أن الوصف إلزامي بعد أول حفظ،
فلا يصحّ مسحه إطلاقاً؟)

---

## 6. المسارات المشمولة — الأدمن والشريك والقراءة العامة

الطلبات 1–3 **لا تخصّ مسار الأدمن وحده**. الشريك هو كاتب الأوصاف الأساسي، والضيف يقرؤها
من المسار العام لا من مسار الأدمن. لو طُبّق الإصلاح على مسار واحد فقط، يبقى نصف المنتج
مكسوراً وبطريقة لن نكتشفها إلا من شكوى شريك:

| الجهة | المسارات | المطلوب عليها |
|---|---|---|
| لوحة الأدمن (وحدات ممسى ومراجعة وحدات الشركاء) | `POST /admin/units` · `PATCH /admin/units/{id}` · `GET /admin/units/{id}` | حفظ `\n` كما هي · `max:2000` بـ `mb_strlen` · بلا أي تحويل |
| لوحة الشريك (إنشاء وتعديل وحداته) | `POST /units` · `PATCH /units/{id}` — نفس المسار، مُميَّز بتوكن الشريك | **نفس القاعدتين حرفياً.** أي حدّ أقل هنا يعني رفض `422` على آخر خطوة في نموذج الشريك |
| موقع ممسى (قراءة الضيف) | `GET /units` · `GET /units/{id}` · `GET /units/popular` | إرجاع `description` بنفس البايتات المخزّنة — هذا هو المصدر الذي يقرأ منه الضيف فعلياً |

لو كانت القاعدة مُعرَّفة في `FormRequest` منفصل لكل مسار، الرجاء التأكد من الثلاثة لا من
واحد. ولو كان المسار العام يمرّ على `Resource` / `Transformer` مختلف، الرجاء التأكد أنه
لا يقصّ ولا يُهرّب الحقل.

---

## الخلاصة

| # | الطلب | يحجب |
|---|---|---|
| 1 | تأكيد أن `\n` تُحفظ وتُسترجع كما هي على كل المسارات | التنسيق كله |
| 2 | رفع حدّ `description` إلى 2000 حرف (`mb_strlen`) على مساري الأدمن والشريك | كل وصف منسّق |
| 3 | تأكيد عدم وجود أي تحويل HTML أو هروب (`htmlspecialchars`) على الحقل | صحّة العرض وسلامة البيانات |
| 4 | تأكيد أن القراءة العامة `GET /units/{id}` تُرجع النصّ كما خُزّن | ما يراه الضيف |

النقاط 1 و2 و4 تحجب الإطلاق. لو الحدّ الحالي على الخادم ليس 500، أخبرونا بالرقم الحقيقي
حتى نطابقه في الواجهة بدل أن نخمّنه مرّة أخرى.

---

## نموذج ردّ سريع

يكفي أن تنسخوا هذا وتملؤوه — لا نحتاج أكثر منه:

```text
1. تخزين \n كما هي:            نعم / لا  (إن لا: أين تُطوى؟)
2. الحدّ الحالي على /admin/units:      ____   وعلى /units (الشريك): ____
3. رُفع إلى 2000:                نعم / لا  (تاريخ النشر: ______)
4. العدّ بـ mb_strlen:            نعم / لا
5. الحدّ الأدنى ما زال 10:         نعم / لا
6. أي htmlspecialchars/strip_tags على الحقل: نعم / لا
7. GET /units/{id} يُرجع النصّ كما خُزّن:  نعم / لا
8. مسح حقل اختياري يتم بإرسال:      null / "" / غير مسموح
```
